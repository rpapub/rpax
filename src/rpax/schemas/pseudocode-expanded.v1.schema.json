{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://rpax.dev/schemas/pseudocode-expanded.v1.schema.json",
  "title": "rpax expanded-pseudocode artifact",
  "description": "Schema for expanded-pseudocode/*.expanded.json artifacts generated by rpax parse",
  "allOf": [
    {
      "$ref": "pseudocode.v1.schema.json"
    }
  ],
  "properties": {
    "expandedPseudocode": {
      "type": "array",
      "description": "Recursively expanded pseudocode lines (InvokeWorkflowFile calls inlined)",
      "items": {
        "type": "string"
      }
    },
    "expansionConfig": {
      "type": "object",
      "description": "Configuration used for recursive expansion",
      "properties": {
        "maxDepth": {
          "type": "integer",
          "minimum": 0,
          "description": "Maximum recursion depth for expansion"
        },
        "cycleHandling": {
          "type": "string",
          "enum": ["detect_and_mark", "detect_and_stop", "ignore"],
          "description": "Strategy for handling cyclic workflow invocations"
        },
        "generatedRecursive": {
          "type": "boolean",
          "description": "True when recursive expansion was performed"
        }
      }
    }
  },
  "additionalProperties": true
}
